# Agent Scrutiny — Development Dependencies
#
# Includes runtime dependencies plus testing, linting, and formatting tools.
# Install with: pip install -r requirements-dev.txt

-r requirements.txt

# Test runner and async support
pytest>=7.4.0
pytest-asyncio>=0.21.0
pytest-cov>=4.1.0

# Formatting and linting (matched by .github/workflows/ci.yml)
black>=23.12.0
isort>=5.13.0
flake8>=7.0.0
mypy>=1.8.0

# Git hooks
pre-commit>=3.6.0
